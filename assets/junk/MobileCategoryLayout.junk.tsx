{
  /* CATEGORY MOBILE VERSION */
}
{
  /* <Accordion>
          <AccordionSummary
            expandIcon={
              <ArrowBackIosNew sx={{ color: "#ff9e0f", fontSize: "14px" }} />
            }
            aria-controls="category-accordion"
            id="category-accordion-header"
          >
            <Box flex={1} display="flex" justifyContent="end">
              <Link href={props.slug}>
                <Typography
                  className={`${vazirmatn.className} category-name`}
                  sx={{
                    color: "#000000",
                    fontSize: "14px",
                    transition: "color ease-in 100ms",
                  }}
                >
                  {props.name}
                </Typography>
              </Link>
            </Box>
            <Box
              ml="5px"
              flex={0.1}
              display="flex"
              justifyContent="center"
              alignItems="center"
            >
              <Image src={props.icon} alt={props.name} width={20} height={20} />
            </Box>
          </AccordionSummary>
          <AccordionDetails>
            {props.subCategories?.map((subCategory) => (
              <CategoryCard key={subCategory.slug} {...subCategory} />
            ))}
          </AccordionDetails>
        </Accordion> */
}
